(async function () {
  if (!(await K.requireStaff())) return;
  const page = K.shell({});
  let day = new URLSearchParams(location.search).get("dag") || K.today();
  function list() { return S.orders.filter(o => o.day === day && (o.statut === "Prête" || o.statut === "Sortie en livraison" || (o.statut === "Facturée" && o.livreeLe && K.isoDay(o.livreeLe) === day))).sort((a, b) => ({ "Sortie en livraison": 0, "Prête": 1, "Facturée": 2 })[a.statut] - ({ "Sortie en livraison": 0, "Prête": 1, "Facturée": 2 })[b.statut] || a.client.localeCompare(b.client, "nl")); }
  function render() {
    const rows = list(); const onRoad = rows.filter(o => o.statut === "Sortie en livraison"), ready = rows.filter(o => o.statut === "Prête"), done = rows.filter(o => o.statut === "Facturée");
    const late = S.orders.filter(o => o.late && o.statut !== "Facturée");
    page.innerHTML = '<div class="page-h"><div><h1 class="h1">Leveringen</h1><p class="sub">' + K.esc(K.dateLong(day)) + ' · ' + rows.length + ' stop' + (rows.length === 1 ? "" : "s") + '</p></div><span class="spacer"></span><div class="opt" style="flex:0 0 auto"><button type="button" data-day="' + K.today() + '"' + (day === K.today() ? ' class="on"' : "") + '>Vandaag</button><button type="button" data-day="' + K.addDays(K.today(), 1) + '"' + (day === K.addDays(K.today(), 1) ? ' class="on"' : "") + '>Morgen</button><input type="date" class="input" id="pickDay" aria-label="Kies een dag" value="' + day + '" style="width:auto;min-height:44px"></div>' + (ready.length ? '<button type="button" class="btn btn-p btn-sm" id="departAll">Ronde vertrekt (' + ready.length + ')</button>' : "") + '</div>' +
      '<div class="content" style="padding-top:14px">' +
      (late.length ? K.c.warn("<b>" + late.length + " bestelling" + (late.length === 1 ? "" : "en") + " te laat</b> — " + late.map(o => '<a href="/order.html?id=' + encodeURIComponent(o.id) + '">' + K.esc(o.client) + '</a>').join(", ")) : "") +
      '<div class="card"><div class="card-h"><div><h2 class="h2">Stops</h2><p class="sub">Onderweg eerst, dan klaar om te vertrekken, dan afgewerkt</p></div><button type="button" class="btn btn-o btn-sm" id="bons">' + K.icon("print") + 'Alle leveringsbonnen</button></div>' +
      (rows.length ? rows.map((o, i) => { const adr = (o.klant && o.klant.adresse || "").replace(/\n/g, ", "); return '<div class="stop"><span class="no">' + (i + 1) + '</span><div style="flex:1;min-width:0"><div style="display:flex;gap:8px;align-items:center;flex-wrap:wrap"><b>' + K.esc(o.client) + '</b>' + (o.paiement !== "Payé" && o.statut !== "Facturée" ? '<span class="tag">te innen ' + K.eur(o.total) + '</span>' : "") + (o.notes ? '<span class="tag" title="' + K.esc(o.notes) + '">nota</span>' : "") + '</div><div class="quiet" style="font-size:12px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis">' + K.esc(adr || "adres onbekend") + ' · ' + K.esc(S.lineTxt(o)) + '</div></div>' + (o.statut === "Facturée" ? '<span class="cell-st c-done">Geleverd</span>' : o.statut === "Sortie en livraison" ? '<span class="cell-st c-road">Onderweg</span>' : '<span class="cell-st c-ready">Klaar</span>') + '<div style="display:flex;gap:6px;flex-wrap:wrap;justify-content:flex-end">' + (adr ? '<a class="btn btn-o btn-sm" target="_blank" rel="noopener" href="https://maps.google.com/?q=' + encodeURIComponent(adr) + '">' + K.icon("map") + 'Kaart</a>' : "") + (o.klant && o.klant.tel ? '<a class="btn btn-o btn-sm" href="tel:' + K.esc(String(o.klant.tel).replace(/\s+/g, "")) + '">Bellen</a>' : "") + '<button type="button" class="btn btn-o btn-sm" data-act="delivery" data-id="' + o.id + '">Bon</button>' + (o.statut === "Facturée" ? '<button type="button" class="btn btn-o btn-sm" data-act="invoice" data-id="' + o.id + '">Factuur</button>' : S.correctBtn(o) + S.nextAction(o)) + '</div></div>'; }).join("") : '<div class="empty" style="margin:12px">Geen leveringen voor ' + K.esc(K.relDay(day).toLowerCase()) + '. Zet bestellingen klaar in het Magazijn.</div>') +
      '<div class="card-b quiet" style="font-size:12.5px;border-top:1px solid var(--line)">Uitzondering onderweg (afwezig, geweigerd, gedeeltelijk)? Noteer het bij de ontvangstbevestiging. Per ongeluk op Vertrekt gedrukt? „Corrigeren” zet de stop terug op Klaar. Automatische routeplanning volgt later.</div></div></div>';
    K.$$("[data-day]", page).forEach(b => { b.onclick = () => { day = b.dataset.day; render(); }; });
    page.querySelector("#pickDay").onchange = e => { if (e.target.value) { day = e.target.value; render(); } };
    page.querySelector("#bons").onclick = () => { const r = list().filter(o => o.statut !== "Facturée"); if (!r.length) { K.toast("Geen bonnen voor deze dag."); return; } r.forEach(o => S.openDoc(o, "delivery")); };
    const da = page.querySelector("#departAll"); if (da) da.onclick = async () => { if (!(await K.confirm({ title: ready.length + " bestellingen vertrekken?", text: "Alles wat klaar staat gaat op Onderweg.", yes: "Vertrekken" }))) return; for (const o of ready) { try { await K.api("/api/updateorder", { json: { id: o.id, statut: "Sortie en livraison", skipStock: true } }); } catch (err) { K.toast(o.client + ": " + err.message, { kind: "err" }); } } await S.load(true); render(); K.toast("Ronde onderweg"); };
  }
  S.bindActions(page, render);
  page.innerHTML = '<div class="page-h"><h1 class="h1">Leveringen</h1></div><div class="content">' + K.c.skeleton(3) + '</div>';
  try { await S.load(); render(); } catch (err) { if (err.status !== 401) page.innerHTML = '<div class="content" style="padding-top:20px">' + K.c.error(err.message) + '</div>'; }
})();
